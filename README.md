# EO2HD Archipelago Plugin Development

Pretty self explanatory. The game runs in a Unity shell, so we can hopefully modify it from there.

# Notes

Will need a patcher component, since it seems like injecting data might be difficult.
The plugin, thus, will just be a listener for events.
